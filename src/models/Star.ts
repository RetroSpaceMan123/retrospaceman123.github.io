export class Star {
    id: number
    left: number
    top: number
    delay: number

    constructor(id: number, left: number, top: number, delay: number) {
        this.id = id
        this.left = left
        this.top = top
        this.delay = delay
    }
}